import { GeneratorVersion } from '../../domain/generation/generator-version'; import { SystemLocator } from '../../domain/generation/procedural-locator'; import { UniverseGenerationKey } from '../../domain/generation/universe-generation-key'; import type { PlanetarySystem } from '../../domain/planetary/planetary-system'; import { SystemSeed } from '../../domain/seed/hierarchical-seeds'; import { UniverseSeed } from '../../domain/universe/universe-seed'; import { TransNeptunianObjectGenerator } from './trans-neptunian-object-generator';
describe('TransNeptunianObjectGenerator point 22.7 V1',()=>{const key=new UniverseGenerationKey(UniverseSeed.parse('7F21-A9D4-18CE-4B70-92F1-6A0C-6E35-D8B1'),GeneratorVersion.V1);const loc=new SystemLocator(6n,113n,9n);const seed=new SystemSeed('22222222222222222222222222222222');function fixture(dust=5,withPlanets=true):PlanetarySystem{return {generationKey:key,locator:loc,seed,planetCount:withPlanets?3:0,hostStellarSystem:{secondaryCompanion:null},orbits:withPlanets?[{semiMajorAxisAu:.15},{semiMajorAxisAu:1.2},{semiMajorAxisAu:5.5}]:[],orbitalLayout:{orbitTopology:'CIRCUMSTELLAR',generationInnerLimitAu:.1,generationOuterLimitAu:50},orbitalPeriodLayout:{gravitatingMassSolar:1},habitableZone:{referenceLuminositySolar:1},formationBlueprint:{centralMassSolar:1,sourceInnerRadiusAu:.1,sourceOuterRadiusAu:50,residualDustMassEarth:dust,sourceCandidateCount:10,sourceMigratedBodyCount:2,sourceCollisionCount:1}} as unknown as PlanetarySystem;}
it('should deterministically materialize a bounded relevant outer population',()=>{const a=TransNeptunianObjectGenerator.generate(key,fixture());const b=TransNeptunianObjectGenerator.generate(key,fixture());expect(b).toEqual(a);expect(a.reservoirSupportIndex01).toBeCloseTo(0.7752724920631788,12);expect(a.relevantObjectCount).toBe(6);expect(a.coldClassicalCount).toBe(3);expect(a.hotClassicalCount).toBe(1);expect(a.resonantCount).toBe(1);expect(a.scatteredCount).toBe(1);expect(a.detachedCount).toBe(0);expect(a.relevantObjects[0].proceduralId).toBe('9E8BC2766DCDABF8A4D800F8700AAE36');expect(a.relevantObjects[0].diameterKilometers).toBeCloseTo(1882.1838947758079,9);expect(a.relevantObjects[0].properties.semiMajorAxisAu).toBeCloseTo(48.825645639328286,10);for(const o of a.relevantObjects){expect(o.properties.eccentricity).toBeLessThan(1);expect(o.properties.periapsisAu).toBeGreaterThan(0);expect('discoveryState' in o).toBe(false);}});
it('should preserve zero dust as an empty outer population',()=>{const a=TransNeptunianObjectGenerator.generate(key,fixture(0));expect(a.reservoirSupportIndex01).toBe(0);expect(a.relevantObjectCount).toBe(0);});
it('should avoid resonant classification when no mature reference planet exists',()=>{const a=TransNeptunianObjectGenerator.generate(key,fixture(5,false));expect(a.relevantObjects.some(o=>o.dynamicalRegime==='RESONANT')).toBe(false);});});
