import { SystemLocator } from '../generation/procedural-locator'; import { SystemSeed } from '../seed/hierarchical-seeds'; import { InterstellarObjectIdentity } from './interstellar-object-identity';
describe('InterstellarObjectIdentity point 22.8',()=>{it('should keep a local encounter identity without creating a child seed',()=>{const seed=new SystemSeed('00000000000000000000000000000155');const x=new InterstellarObjectIdentity(new SystemLocator(1n,2n,3n),seed,1,'AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA');expect(x.encounterSystemSeed).toBe(seed);expect(x.localDesignation).toBe('ISO-001');expect('seed' in x).toBe(false);});});
